import { UnknownBreedPipe } from './unknown-breed.pipe';

describe('UnknownBreedPipe', () => {
  it('create an instance', () => {
    const pipe = new UnknownBreedPipe();
    expect(pipe).toBeTruthy();
  });
});
